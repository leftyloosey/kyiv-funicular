import { SelectOnClick } from './select-on-click';

describe('SelectOnClick', () => {
  it('should create an instance', () => {
    const directive = new SelectOnClick();
    expect(directive).toBeTruthy();
  });
});
